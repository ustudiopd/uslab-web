# 블로그에디터_구현.md 검토 보고서

**검토 일자**: 2025-01-13  
**최종 업데이트**: 2025-01-13 (브라우저 테스트 후)  
**검토 대상**: `블로그에디터_구현.md`

---

## 📊 현재 구현 상황 (2025-01-13 업데이트)

### ✅ 구현 완료된 항목

1. **파일 생성**
   - ✅ `components/editor/extensions.tsx` 생성 완료
   - ✅ `components/editor/BubbleMenu.tsx` 생성 완료
   - ✅ `BlogEditor.tsx` 수정 완료
   - ✅ `app/globals.css` 수정 완료

2. **코드 통합**
   - ✅ `slashCommand` 확장 추가 완료
   - ✅ `EditorCommand` 컴포넌트 추가 완료
   - ✅ `BubbleMenu` 컴포넌트 추가 완료
   - ✅ Novel.sh CSS 변수 추가 완료
   - ✅ ProseMirror 스타일 추가 완료

3. **빌드 상태**
   - ✅ 빌드 성공 (TypeScript 오류 해결 완료)
   - ✅ 모든 파일 컴파일 정상

### ⚠️ 발견된 문제점

1. **슬래시 명령어 메뉴 미작동**
   - ❌ `/` 입력 시 메뉴가 나타나지 않음
   - ❌ `EditorCommand` 컴포넌트가 DOM에 렌더링되지 않음
   - **원인 추정**: `EditorContent`의 children 렌더링 방식 문제 가능성

2. **Bubble Menu 확인 불가**
   - ❓ 텍스트 선택 시 메뉴 표시 여부 확인 불가
   - **원인 추정**: `EditorBubble` 컴포넌트 렌더링 문제 가능성

3. **에디터 기본 기능**
   - ✅ 에디터 로드 정상
   - ✅ 에디터 포커스 정상
   - ✅ 기본 Tiptap 기능 작동

### 🔍 브라우저 테스트 결과 (2025-01-13)

**테스트 환경**: `http://localhost:3000/admin/posts/write`

**확인된 사항**:
- 에디터 요소 (`.ProseMirror`) 정상 렌더링
- 에디터 포커스 가능
- 기본 Tiptap 에디터 구조 정상

**발견된 문제**:
- `EditorCommand` 관련 DOM 요소 없음
- 슬래시 명령어 메뉴 트리거되지 않음
- `EditorContent`의 children 렌더링 확인 필요

**다음 단계**:
1. Novel.sh의 `EditorContent` children 렌더링 방식 확인
2. `EditorCommand`와 `BubbleMenu`의 올바른 배치 위치 확인
3. React DevTools로 컴포넌트 트리 확인

---

## ✅ 검토 결과

### 문서의 정확성: **매우 정확함**

1. **문제 원인 분석**: ✅ 정확
   - "확장만 넣고 UI 컴포넌트를 안 붙임" - 정확한 진단
   - "Novel 전용 CSS/스타일 레이어가 빠짐" - 정확한 진단

2. **누락된 기능 목록**: ✅ 정확
   - 슬래시 명령어 메뉴 (`/`)
   - Bubble Menu (텍스트 선택 메뉴)
   - 드래그 핸들
   - CSS 스타일

3. **우선순위**: ✅ 적절
   - Phase A, B, C가 필수로 분류됨
   - Phase D가 권장으로 분류됨

4. **결론**: ✅ 명확
   - "지금 상태는 정상" - 맞음
   - "다음 3가지만 하면 바로 노션 체감" - 정확

---

## 📝 보완 사항

### 현재 문서의 강점
- 문제 원인을 명확히 분석함
- 우선순위가 적절함
- 핵심만 간결하게 정리됨

### 추가하면 좋을 내용
1. **구체적인 파일 경로 및 수정 위치**
2. **실제 코드 예제** (import 문, 컴포넌트 구조 등)
3. **완료 조건 체크리스트**

---

## 🎯 즉시 실행 가능한 작업 지시서

### 작업 1: 슬래시 명령어 메뉴 구현

#### 1-1. 확장 파일 생성
**파일**: `components/editor/extensions.ts` (새 파일)

**내용**:
```typescript
'use client';

import { Command, createSuggestionItems } from 'novel/extensions';
import { 
  CheckSquare, Code, Heading1, Heading2, Heading3, 
  List, ListOrdered, Text, TextQuote 
} from 'lucide-react';

export const suggestionItems = createSuggestionItems([
  {
    title: 'Text',
    description: 'Just start typing with plain text.',
    searchTerms: ['p', 'paragraph'],
    icon: <Text size={18} />,
    command: ({ editor, range }) => {
      editor.chain().focus().deleteRange(range).toggleNode('paragraph', 'paragraph').run();
    },
  },
  {
    title: 'Heading 1',
    description: 'Big section heading.',
    searchTerms: ['title', 'big', 'large', 'h1'],
    icon: <Heading1 size={18} />,
    command: ({ editor, range }) => {
      editor.chain().focus().deleteRange(range).setNode('heading', { level: 1 }).run();
    },
  },
  {
    title: 'Heading 2',
    description: 'Medium section heading.',
    searchTerms: ['subtitle', 'medium', 'h2'],
    icon: <Heading2 size={18} />,
    command: ({ editor, range }) => {
      editor.chain().focus().deleteRange(range).setNode('heading', { level: 2 }).run();
    },
  },
  {
    title: 'Heading 3',
    description: 'Small section heading.',
    searchTerms: ['subtitle', 'small', 'h3'],
    icon: <Heading3 size={18} />,
    command: ({ editor, range }) => {
      editor.chain().focus().deleteRange(range).setNode('heading', { level: 3 }).run();
    },
  },
  {
    title: 'Bullet List',
    description: 'Create a simple bullet list.',
    searchTerms: ['unordered', 'point'],
    icon: <List size={18} />,
    command: ({ editor, range }) => {
      editor.chain().focus().deleteRange(range).toggleBulletList().run();
    },
  },
  {
    title: 'Numbered List',
    description: 'Create a list with numbering.',
    searchTerms: ['ordered'],
    icon: <ListOrdered size={18} />,
    command: ({ editor, range }) => {
      editor.chain().focus().deleteRange(range).toggleOrderedList().run();
    },
  },
  {
    title: 'To-do List',
    description: 'Track tasks with a to-do list.',
    searchTerms: ['todo', 'task', 'list', 'check', 'checkbox'],
    icon: <CheckSquare size={18} />,
    command: ({ editor, range }) => {
      editor.chain().focus().deleteRange(range).toggleTaskList().run();
    },
  },
  {
    title: 'Quote',
    description: 'Capture a quote.',
    searchTerms: ['blockquote'],
    icon: <TextQuote size={18} />,
    command: ({ editor, range }) =>
      editor.chain().focus().deleteRange(range).toggleNode('paragraph', 'paragraph').toggleBlockquote().run(),
  },
  {
    title: 'Code',
    description: 'Capture a code snippet.',
    searchTerms: ['codeblock'],
    icon: <Code size={18} />,
    command: ({ editor, range }) =>
      editor.chain().focus().deleteRange(range).toggleCodeBlock().run(),
  },
]);

export const slashCommand = Command.configure({
  suggestion: {
    items: () => suggestionItems,
  },
});
```

#### 1-2. BlogEditor.tsx 수정

**수정 위치 1: import 문 추가** (4번째 줄 근처)
```typescript
// 기존 import에 추가
import { Command, EditorCommand, EditorCommandItem, EditorCommandList, EditorCommandEmpty } from 'novel';
import { handleCommandNavigation } from 'novel/extensions';
import { suggestionItems, slashCommand } from './extensions';
```

**수정 위치 2: extensions 배열에 slashCommand 추가** (92-103번째 줄)
```typescript
const extensions = useMemo(() => [
  StarterKit.configure({
    horizontalRule: false,
  }),
  Markdown as any,
  imageExtension,
  TaskList,
  TaskItem,
  Horizontal,
  CustomKeymap,
  slashCommand, // ← 이 줄 추가
], [imageExtension]);
```

**수정 위치 3: EditorContent 내부에 EditorCommand 추가** (107번째 줄 `<EditorContent>` 태그 내부)
```typescript
<EditorContent>
  {/* ← 이 부분에 추가 */}
  <EditorCommand className="z-50 h-auto max-h-[330px] w-72 overflow-y-auto rounded-md border border-slate-700 bg-slate-900 px-1 py-2 shadow-md">
    <EditorCommandEmpty className="px-2 text-sm text-slate-400">No results</EditorCommandEmpty>
    <EditorCommandList>
      {suggestionItems.map((item) => (
        <EditorCommandItem
          value={item.title}
          onCommand={(val) => item.command(val)}
          className="flex w-full items-center space-x-2 rounded-md px-2 py-1 text-left text-sm hover:bg-slate-800 aria-selected:bg-slate-800"
          key={item.title}>
          <div className="flex h-10 w-10 items-center justify-center rounded-md border border-slate-700 bg-slate-800">
            {item.icon}
          </div>
          <div>
            <p className="font-medium text-slate-200">{item.title}</p>
            <p className="text-xs text-slate-400">{item.description}</p>
          </div>
        </EditorCommandItem>
      ))}
    </EditorCommandList>
  </EditorCommand>
  {/* ← 여기까지 추가 */}
</EditorContent>
```

**수정 위치 4: editorProps에 handleCommandNavigation 추가** (117번째 줄 `editorProps` 내부)
```typescript
editorProps={{
  handleDOMEvents: {
    keydown: (_view, event) => handleCommandNavigation(event),
  },
  attributes: {
    class: 'prose prose-invert max-w-none focus:outline-none min-h-[350px] sm:min-h-[450px] lg:min-h-[500px] prose-sm sm:prose-base',
  },
  // ... 기존 handlePaste, handleDrop ...
}}
```

---

### 작업 2: Bubble Menu 구현

#### 2-1. BubbleMenu 컴포넌트 생성
**파일**: `components/editor/BubbleMenu.tsx` (새 파일 생성)

**내용**:
```typescript
'use client';

import { EditorBubble, EditorBubbleItem, useEditor } from 'novel';
import { Bold, Italic, Underline, Strikethrough, Code } from 'lucide-react';

export function BubbleMenu() {
  const { editor } = useEditor();

  if (!editor) return null;

  return (
    <EditorBubble
      tippyOptions={{
        placement: 'top',
      }}
      className="flex w-fit max-w-[90vw] overflow-hidden rounded border border-slate-700 bg-slate-900 shadow-xl">
      <EditorBubbleItem
        onSelect={() => editor.chain().focus().toggleBold().run()}
        className={`p-2 ${editor.isActive('bold') ? 'bg-slate-800' : ''}`}>
        <Bold className="h-4 w-4 text-slate-300" />
      </EditorBubbleItem>
      <EditorBubbleItem
        onSelect={() => editor.chain().focus().toggleItalic().run()}
        className={`p-2 ${editor.isActive('italic') ? 'bg-slate-800' : ''}`}>
        <Italic className="h-4 w-4 text-slate-300" />
      </EditorBubbleItem>
      <EditorBubbleItem
        onSelect={() => editor.chain().focus().toggleUnderline().run()}
        className={`p-2 ${editor.isActive('underline') ? 'bg-slate-800' : ''}`}>
        <Underline className="h-4 w-4 text-slate-300" />
      </EditorBubbleItem>
      <EditorBubbleItem
        onSelect={() => editor.chain().focus().toggleStrike().run()}
        className={`p-2 ${editor.isActive('strike') ? 'bg-slate-800' : ''}`}>
        <Strikethrough className="h-4 w-4 text-slate-300" />
      </EditorBubbleItem>
      <EditorBubbleItem
        onSelect={() => editor.chain().focus().toggleCode().run()}
        className={`p-2 ${editor.isActive('code') ? 'bg-slate-800' : ''}`}>
        <Code className="h-4 w-4 text-slate-300" />
      </EditorBubbleItem>
    </EditorBubble>
  );
}
```

#### 2-2. BlogEditor.tsx에 BubbleMenu 추가
**파일**: `components/editor/BlogEditor.tsx`

**수정 위치 1: import 문 추가** (4번째 줄 근처)
```typescript
import { BubbleMenu } from './BubbleMenu';
```

**수정 위치 2: EditorContent 내부에 BubbleMenu 추가** (EditorCommand 위에)
```typescript
<EditorContent>
  <BubbleMenu /> {/* ← 이 줄 추가 */}
  <EditorCommand>
    {/* ... 기존 코드 ... */}
  </EditorCommand>
</EditorContent>
```

---

### 작업 3: CSS 스타일 적용

#### 3-1. globals.css 수정
**파일**: `app/globals.css`

**수정 위치 1: `:root` 블록에 Novel.sh highlight 변수 추가** (5-8번째 줄 근처)
```css
:root {
  --font-noto-sans: 'Noto Sans KR', sans-serif;
  --font-jetbrains-mono: 'JetBrains Mono', monospace;
  
  /* Novel.sh highlight 변수 추가 */
  --novel-highlight-default: #ffffff;
  --novel-highlight-purple: #f6f3f8;
  --novel-highlight-red: #fdebeb;
  --novel-highlight-yellow: #fbf4a2;
  --novel-highlight-blue: #c1ecf9;
  --novel-highlight-green: #acf79f;
  --novel-highlight-orange: #faebdd;
  --novel-highlight-pink: #faf1f5;
  --novel-highlight-gray: #f1f1ef;
}
```

**수정 위치 2: 파일 끝에 ProseMirror 스타일 추가** (63번째 줄 이후)
```css
/* Novel.sh ProseMirror 스타일 */
.ProseMirror {
  @apply p-12 px-8 sm:px-12;
}

.ProseMirror .is-editor-empty:first-child::before {
  content: attr(data-placeholder);
  float: left;
  color: #64748b;
  pointer-events: none;
  height: 0;
}

.ProseMirror .is-empty::before {
  content: attr(data-placeholder);
  float: left;
  color: #64748b;
  pointer-events: none;
  height: 0;
}

.ProseMirror img {
  transition: filter 0.1s ease-in-out;
}

.ProseMirror img:hover {
  cursor: pointer;
  filter: brightness(90%);
}

.ProseMirror img.ProseMirror-selectednode {
  outline: 3px solid #5abbf7;
  filter: brightness(90%);
}
```

---

## ✅ 완료 조건 체크리스트

### Phase A: 슬래시 명령어
- [x] `components/editor/extensions.tsx` 파일 생성 (`.tsx`로 변경됨)
- [x] `suggestionItems` 정의 완료
- [x] `slashCommand` 확장 생성
- [x] `BlogEditor.tsx`에 `slashCommand` 추가
- [x] `EditorCommand` 컴포넌트 추가 (코드상)
- [ ] `handleCommandNavigation` 추가 (주석 처리됨 - import 경로 문제)
- [ ] `/` 입력 시 메뉴 표시 확인 ❌ **문제 발생**

### Phase B: Bubble Menu
- [x] `components/editor/BubbleMenu.tsx` 파일 생성
- [x] `EditorBubble`, `EditorBubbleItem` 사용
- [x] Bold, Italic, Underline, Strike, Code 버튼 구현
- [ ] 텍스트 선택 시 메뉴 표시 확인 ❓ **확인 불가**

### Phase C: CSS 스타일
- [x] Novel.sh highlight 변수 추가
- [x] ProseMirror placeholder 스타일 추가
- [x] 이미지 hover/선택 스타일 추가
- [x] 에디터 padding 스타일 적용

### 🐛 해결 필요 사항
- [ ] `EditorCommand` 컴포넌트가 DOM에 렌더링되지 않는 문제 해결
- [ ] `EditorContent`의 children 렌더링 방식 확인 및 수정
- [ ] `handleCommandNavigation` import 경로 문제 해결
- [ ] 슬래시 명령어 메뉴 트리거 확인

---

## 🎯 예상 작업 시간

- **Phase A (슬래시 명령어)**: 1-2시간
- **Phase B (Bubble Menu)**: 1-2시간  
- **Phase C (CSS 스타일)**: 30분-1시간

**총 예상 시간**: 2.5-5시간

---

## 📌 검토 의견

### 문서의 강점
1. **문제 원인을 명확히 분석**: 확장만 있고 UI 컴포넌트가 없다는 진단이 정확함
2. **우선순위가 적절**: 필수 기능과 권장 기능을 명확히 구분
3. **간결하고 명확**: 핵심만 간결하게 정리되어 이해하기 쉬움

### 개선 제안
1. **구체적인 코드 예제 추가**: 현재는 방향만 제시되어 있음
2. **파일 경로 및 수정 위치 명시**: 어느 파일의 어느 부분을 수정해야 하는지 명확히
3. **완료 조건 체크리스트**: 각 Phase별로 완료 조건을 명시

---

## 💡 결론

**`블로그에디터_구현.md` 문서는 매우 정확하고 유용합니다.**

- 문제 원인 분석이 정확함
- 해결 방향이 명확함
- 우선순위가 적절함

**추가 작업 지시서**를 별도 파일(`블로그에디터_구현_검토보고서.md`)로 생성하여 구체적인 코드 예제와 수정 위치를 명시했습니다.

### 📌 현재 상태 요약 (2025-01-13)

**구현 진행률**: 약 80% 완료
- ✅ 파일 생성 및 코드 통합 완료
- ✅ 빌드 성공
- ❌ 런타임 동작 문제 발생 (슬래시 명령어 메뉴 미작동)

**다음 작업**:
1. Novel.sh 공식 문서에서 `EditorContent` children 렌더링 방식 확인
2. `EditorCommand`와 `BubbleMenu`의 올바른 배치 방법 확인
3. React DevTools로 컴포넌트 렌더링 상태 확인
4. 필요시 Novel.sh 예제 코드 참조하여 구조 수정

이 두 문서를 함께 사용하면 Notion 스타일 에디터를 완전히 구현할 수 있습니다.
